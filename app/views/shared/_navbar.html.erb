<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm p-2 mb-3 bg-body">
  <div class="container-fluid">
    <%= link_to "Donate Me", root_path, class: "navbar-brand fw-bolder text-decoration-none fs-3" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="navbar-nav mb-2 mb-lg-0">
        <% if current_user && user_signed_in? %>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-link active fw-bold" %> 
        <% else %>
          <li class="nav-item">
            <%= link_to "Log In", new_user_session_path, class: "nav-link active fw-bold" %>
          </li> 
        
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link active fw-bold" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>